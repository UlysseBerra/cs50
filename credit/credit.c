#include <cs50.h>
#include <stdio.h>

bool luhn(long n);

int main(void)
{
    string n = get_string("Number: ");

    if (n[-1] == n[12])
}

bool luhn(long n)
{

    bool result;
    return result;
}

