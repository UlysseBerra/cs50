#include <cs50.h>
#include <stdio.h>

bool luhn(long n);

int main(void)
{
    string n = get_string("Number: ");
    x = n / 10;
    printf("%")
}

bool luhn(long n)
{

    bool result;
    return result;
}

